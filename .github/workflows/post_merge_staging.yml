name: _Tag
on:
  pull_request:
    types: [closed]
    branches:
      - qcom-next-staging-test
    
jobs:
  tag:
    if: github.event.pull_request.merged == true
    uses: sgaud-quic/kernel-config/.github/workflows/post_merge_staging.yml@main
    secrets: inherit
